/**
 * Variables de entorno para PRODUCCIÓN.
 * En build production, Angular reemplaza `environment.ts` por este archivo.
 */
export const environment = {
  production: true,
  apiUrl: 'https://arcash.me/api',
};
