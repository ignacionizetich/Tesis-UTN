import { Component } from '@angular/core';

/** Footer de créditos/autores en shell (`app.html`). Distinto de `app-global-footer` (contacto en auth). */
@Component({
  selector: 'app-footer',
  imports: [],
  templateUrl: './footer.html',
  styleUrl: './footer.css'
})
export class Footer {

}
