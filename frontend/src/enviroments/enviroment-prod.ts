/**
 * Este es el archivo de variables de entorno para PRODUCCIÓN.
 * Cuando corres `ng build --configuration production`, Angular
 * REEMPLAZA el archivo `environment.ts` por este.
 */
export const environment = {
    production: true,
    apiUrl: 'https://arcash.me/api' // URL de tu API de producción
};