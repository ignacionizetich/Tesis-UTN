/**
 * Este es el archivo de variables de entorno para DESARROLLO.
 * Cuando corres `ng serve`, Angular usa este archivo.
 */
export const environment = {
    production: false,
    apiUrl: '/api' 
};